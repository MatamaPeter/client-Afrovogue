if (user) {
        setMessage(`Welcome back, ${user.name}! You're now logged in.`);
        // Note: Using in-memory storage instead of localStorage for demo
        // In a real app, you'd use proper authentication state management
        navigate("/");
      } else {
        setMessage("Invalid email or password. Please try again.");
      }
      setIsSubmitting(false);
    }, 1500);
=======
    setTimeout(() => {
      if (user) {
        setMessage(`Welcome back, ${user.name}! You're now logged in.`);
        // Set loggedInUser in localStorage for Header component to detect
        localStorage.setItem('loggedInUser', JSON.stringify(user));
        navigate("/");
      } else {
        setMessage("Invalid email or password. Please try again.");
      }
      setIsSubmitting(false);
    }, 1500);
